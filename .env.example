# Copy this file to ".env" (untracked, see .gitignore) for local builds.
# In Vercel, set these as real Project Environment Variables instead —
# do not commit real values here or into .env.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key
